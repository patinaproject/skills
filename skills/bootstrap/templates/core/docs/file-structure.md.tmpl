# Repository File Structure

Contributor reference for the repository layout. For user-facing install and usage, start with [`../README.md`](../README.md).

## Top level

- `.claude/`: project-level Claude Code configuration
- `.github/`: PR + issue templates and `CODEOWNERS`
- `.husky/`: Git hooks (`commit-msg`, `pre-commit`)
- `skills/`: installable skill packages (when applicable)
- `docs/`: repository docs, design docs, planning artifacts
- Root tooling: `package.json`, `pnpm-lock.yaml`, `commitlint.config.js`, `.markdownlint.jsonc`, `.markdownlintignore`
- Root docs: `AGENTS.md`, `CLAUDE.md`, `CONTRIBUTING.md`, `README.md` (plus `SECURITY.md` for public repos)

## Skills

Each skill lives in its own directory under `skills/` with `SKILL.md` as the main contract.

## Docs

Use `docs/` for durable repository documentation. For Superpowers-driven work:

- `docs/superpowers/specs/` — design documents
- `docs/superpowers/plans/` — implementation plans
