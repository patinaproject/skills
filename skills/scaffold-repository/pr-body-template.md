<!--
  Replace this comment with one closing line for each completed issue.
  Include both the GitHub issue and its corresponding Linear issue.
  Give each reference its own `Closes`, `Fixes`, or `Resolves` keyword,
  chosen independently.

  Example: Closes #123, closes https://linear.app/WORKSPACE/issue/TEAM-123/title

  Retrieve the corresponding IDs through docs/issue-tracker.md and linked issue
  data. If either ID is unavailable, mark the closing line incomplete until
  retrieved. A line missing either reference or either keyword is incomplete.
  Use a form accepted by the repository's integration and closing-reference check.
-->
