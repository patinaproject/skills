---
name: tdd
description: fix a bug by writing the failing test first, then the fix
disable-model-invocation: true
---

Invoke the `tdd` skill and follow it. Resolve Claude tool names, `claude-*` model slugs, and Claude built-in skills through `patina-mode/references/codex-tools.md`, including its Per-skill notes.
