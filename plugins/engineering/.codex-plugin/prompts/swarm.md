---
name: swarm
description: fan out N parallel workers across slices or races, then return one aggregated report
disable-model-invocation: true
---

Invoke the `swarm` skill and follow it. Resolve Claude tool names, `claude-*` model slugs, and Claude built-in skills through `patina-mode/references/codex-tools.md`, including its Per-skill notes.
