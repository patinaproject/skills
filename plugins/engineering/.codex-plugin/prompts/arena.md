---
name: arena
description: run N parallel attempts at the same task and pick the best parts
disable-model-invocation: true
---

Invoke the `arena` skill and follow it. Resolve Claude tool names, `claude-*` model slugs, and Claude built-in skills through `patina-mode/references/codex-tools.md`, including its Per-skill notes.
