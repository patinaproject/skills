---
name: fix-ci
description: find failing PR checks, inspect logs, apply focused fixes
disable-model-invocation: true
---

Invoke the `fix-ci` skill and follow it. Resolve Claude tool names, `claude-*` model slugs, and Claude built-in skills through `patina-mode/references/codex-tools.md`, including its Per-skill notes.
