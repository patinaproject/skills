* text=auto eol=lf

# Canonical skill overlay and Claude skill-loader symlinks are workspace-only.
# They must not leak into release tarballs produced by git archive.
.agents/skills/** export-ignore
.claude/skills/** export-ignore
